# План исследований (Research TODO)

## 1. Проблема холодного старта и развития ML-моделей
- [ ] Провести анализ эволюции размеров моделей машинного обучения (от ранних CNN/RNN до современных LLM, таких как Llama 3, Falcon, GPT-4).
- [ ] Собрать статистику по увеличению весов моделей (в гигабайтах) за последние 5 лет.
- [ ] Описать влияние размера модели на время её загрузки (холодный старт) в бессерверных (Serverless) средах Kubernetes.
- [ ] Исследовать, как медленный старт влияет на SLA (Service Level Agreement) и пользовательский опыт (UX).

## 2. Анализ существующих решений для распределенного кэширования
- [ ] Проанализировать Alluxio: архитектура, плюсы, минусы, проблемы со стоимостью и сложностью интеграции.
- [ ] Проанализировать JuiceFS: архитектура, зависимость от внешних хранилищ (S3, Redis), ограничения по производительности в Serverless.
- [ ] Найти и проанализировать 2-3 альтернативных решения (например, Dragonfly, CubeFS, Rook/Ceph).
- [ ] Сформулировать основные проблемы существующих систем: высокая стоимость, избыточная сложность, проблемы с масштабированием в k8s, единая точка отказа.

## 3. Этап NFS (Network File System)
- [ ] Описать процесс синхронизации при скачивании весов в общую директорию NFS.
- [ ] Исследовать механизм `flock` (file lock) и особенности его работы поверх NFS (`flock over nfs`).
- [ ] Разработать и описать подход для обеспечения консистентности: как гарантировать, что только одна реплика (pod) качает данные, а остальные ждут.
- [ ] Изучить механизм Lease и Leader Election в Kubernetes: как они работают, как применяются для распределенных блокировок.

## 4. Этап P2P кэширования (новая архитектура)
- [ ] Провести подробный обзор алгоритмов P2P-сетей для обнаружения узлов:
  - [ ] Gossip protocol (эпидемический протокол).
  - [ ] Локальный широковещательный поиск (mDNS / UDP Broadcast).
  - [ ] Периодические обновления записи (Heartbeat/Keep-alive) в централизованном хранилище (Redis).
- [ ] Обосновать выбор Redis как оптимального решения для хранения метаданных (простота записи IP-адресов и метаданных чанков).
- [ ] Сравнить протоколы взаимодействия между узлами: UDS (Unix Domain Socket) против TCP.
  - [ ] Почему UDS используется для связи `storage-mounter` и `storage-agent` на одной ноде (скорость, отсутствие сетевого стека).
  - [ ] Почему TCP используется для связи между `storage-agent` на разных нодах.
- [ ] Изучить принципы работы FUSE (Filesystem in Userspace) для создания виртуальной файловой системы, перехватывающей системные вызовы от `vllm`.
