= ОЦЕНКА ЭФФЕКТИВНОСТИ И НАДЁЖНОСТІ ПРЕДЛОЖЕННОГО ПОДХОДА

В данном разделе описывается методика экспериментальной оценки предложенной архитектуры, формулируются метрики, строится расчётная модель задержки, анализируются ожидаемые результаты сравнения и рассматриваются ограничения подхода.

== Методика экспериментов

*Тестовый стенд*

Для сравнительной оценки предлагается следующая конфигурация тестового стенда. Кластер состоит из 6~worker-нод на базе x86\_64-серверов с процессорами Intel Xeon Silver (16 физических ядер, 32 потока), 64~ГБ ОЗУ и локальным NVMe-диском Samsung~980~Pro ёмкостью 2~ТБ (последовательная скорость чтения до 7000~МБ/с). Сеть между нодами --- 10~Гбит/с Ethernet с коммутатором Top-of-Rack, RTT между нодами не превышает 0.5~мс.

Redis развёртывается в конфигурации Redis Sentinel: 1 master, 2 replica, автоматический failover при недоступности master. Размер чанка зафиксирован на уровне 16~МБ как компромисс между накладными расходами на метаданные (меньший чанк --- больше записей в Redis) и гранулярностью параллельной загрузки (более крупный чанк снижает степень P2P-параллелизма).

Набор тестовых моделей включает: Llama~3~8B ($approx$15~ГБ, $approx$960 чанков), Mistral~7B ($approx$14~ГБ, $approx$896 чанков) и Llama~3~70B ($approx$140~ГБ, $approx$8960 чанков). Для воспроизводимости результатов в отсутствие GPU runtime-контейнер заменяется синтетическим читателем, который последовательно читает все файлы модели через FUSE-точку монтирования и фиксирует время до первого байта и общее время чтения.

*Процедура эксперимента*

Каждый сценарий тестируется в двух режимах: _cold cluster_ (дисковые кэши очищены, Redis пуст) и _warm cluster_ (модель загружена хотя бы на одной ноде). В режиме warm cluster дополнительно разделяются сценарии: warm on same node (модель на ноде, где стартует под) и warm on peer node (модель на другой ноде кластера). Для burst-сценария одновременно запускаются 10~подов на разных нодах, и измеряется суммарный внешний трафик и распределение $T_"model_ready"$ по репликам.

*Сравниваемые сценарии*

Сравниваются три подхода:

+ *Baseline* (`storage-initializer + emptyDir`): каждый под независимо загружает всю модель из HuggingFace Hub через публичный интернет-канал. Представляет текущую практику в кластерах без выделенного кэша.

+ *NFS*: общая директория монтируется через PVC/NFS. Первый под загружает модель из репозитория в NFS, остальные читают напрямую из общего тома. Представляет промежуточный подход, описанный в разделе~3.

+ *P2P/FUSE* (предложенный подход): каждый под монтирует виртуальную файловую систему через `storage-mounter`; FUSE-операции транслируются в chunk-запросы к `storage-agent`. Агент обслуживает запросы из local cache, затем из peer cache (TCP к соседним агентам), и только при отсутствии чанков в кластере --- из HuggingFace (remote fallback).

Каждый эксперимент повторяется не менее 5~раз; фиксируются медиана и 99-й перцентиль (p99) каждой метрики. Между экспериментами кэши очищаются или намеренно прогреваются в зависимости от измеряемого сценария. Результаты записываются средствами Prometheus и Grafana; временна́я метка создания пода берётся из Kubernetes Event API.

== Метрики оценки

Для каждого сценария измеряются следующие метрики.

*Время до первого байта* $T_"first_byte"$ --- интервал от момента создания пода до момента, когда runtime-контейнер успешно считал первый байт файла весов. Эта метрика критична для реализации lazy loading, при котором инференс может начаться до полной загрузки модели.

*Время готовности модели* $T_"model_ready"$ --- полное время от создания пода до момента, когда синтетический читатель завершил последовательное чтение всех файлов модели. Для реального ML-runtime эквивалентно времени загрузки весов в GPU-память.

*Cache hit rate* $H_l$ определяется как доля запросов к `storage-agent`, обслуженных из локального дискового кэша:

$ H_l = N_"local_hit" / (N_"local_hit" + N_"peer_hit" + N_"remote") $

*Peer hit rate* $H_p$ --- доля запросов, удовлетворённых с соседних агентов по TCP:

$ H_p = N_"peer_hit" / (N_"local_hit" + N_"peer_hit" + N_"remote") $

*Remote fallback rate* $R = 1 - H_l - H_p$ --- доля запросов, потребовавших обращения к внешнему репозиторию.

*Внешний сетевой трафик* $V_"ext"$ --- суммарный объём данных, скачанный из HuggingFace при масштабировании до $N$ реплик. Для baseline: $V_"ext" = N times S_"model"$; для P2P/FUSE в идеальном случае: $V_"ext" = S_"model"$ (однократная загрузка).

*Redis lookup latency* --- время выполнения команд `SMEMBERS chunk:peers:{hash}` и `HGET agent:{id}`, измеренное как p50 и p99. Ожидаемые значения: p50 $<$~1~мс, p99 $<$~5~мс при развёртывании Redis в той же сети кластера @redis_sentinel_docs.

*CPU overhead FUSE* --- дополнительная нагрузка на CPU worker-ноды, вносимая FUSE-демоном `storage-mounter`, при последовательном чтении 16-МБ чанков.

*Задержка репликации чанка* --- время от записи чанка первым агентом до его появления в реестре Redis. Ожидаемое значение не превышает $T_l + "RTT"_"Redis" approx 2.3 + 1 approx 3.3$~мс.

== Расчётная модель задержки

*Трёхуровневая модель AMAT*

Среднее время доступа к данным (AMAT, Average Memory Access Time) для трёхуровневой иерархии кэша @hennessy2011ca определяется формулой:

$ T_"AMAT" = H_l dot T_l + (1 - H_l) [H_p dot T_p + (1 - H_p) dot T_r] $ <eq-amat-5>

где $T_l$ --- время чтения из локального дискового кэша, $T_p$ --- время получения чанка от соседнего агента по TCP, $T_r$ --- время загрузки чанка из внешнего репозитория, $H_l$ и $H_p$ --- доли попаданий на каждом уровне.

*Характерные значения компонентов задержки*

Для NVMe-диска при последовательном чтении чанка размером 16~МБ:

$ T_l = (16 times 10^6 " байт") / (7000 times 10^6 " байт/с") approx 2.3 " мс" $

Для P2P-передачи чанка по сети 10~Гбит/с с RTT 0.5~мс:

$ T_p = (16 times 10^6 times 8 " бит") / (10^10 " бит/с") + "RTT" approx 12.8 " мс" + 0.5 " мс" approx 13.3 " мс" $

Для загрузки чанка из HuggingFace Hub через типичный канал 200~МБ/с:

$ T_r = (16 times 10^6 " байт") / (200 times 10^6 " байт/с") + T_"internet-RTT" approx 80 " мс" + 50 " мс" approx 130 " мс" $

При перегруженном репозитории или медленном внешнем канале $T_r$ может достигать 500--5000~мс.

*Расчёт AMAT при различных сценариях*

Подставляя характерные значения $T_l approx 2.3$~мс, $T_p approx 13.3$~мс, $T_r approx 130$~мс в формулу (@eq-amat-5):

#figure(
  table(
    columns: 5,
    stroke: (x, y) => if y == 0 or y == 1 { (bottom: 0.5pt) } else { none },
    table.header(
      [$H_l$], [$H_p$], [$R$], [Описание сценария], [AMAT, мс],
    ),
    [0.00], [0.00], [1.00], [Первый запуск, нет кэша], [$approx 130$],
    [0.00], [0.80], [0.20], [Нет локального, есть соседи], [$approx 37$],
    [0.90], [0.80], [0.02], [После прогрева кластера], [$approx 4.3$],
    [0.99], [0.80], [0.002], [Горячий кэш, популярная модель], [$approx 2.5$],
  ),
  caption: [Расчётные значения AMAT при разных уровнях hit rate],
) <tab-amat-calc>

Таблица~@tab-amat-calc демонстрирует, что после прогрева кластера (hit rate 90%) среднее время доступа к чанку снижается примерно в 30~раз по сравнению со сценарием без кэша. При горячем кэше (hit rate 99%) достигается ускорение более чем в 50~раз.

*Время загрузки модели и применение формулы P2P*

Полное время загрузки модели размером $S$ при параллельной загрузке с $k$ пиров @li2020caching:

$ T_"download" = S / (B_"local" + sum_(i=1)^k B_i) + "RTT" $ <eq-p2p-dl>

где $B_"local"$ --- скорость чтения с локального диска, $B_i$ --- пропускная способность $i$-го пира.

Для модели Llama~3~8B ($S = 15$~ГБ):

- *Baseline*: $k = 0$, $B_"HF" = 200$~МБ/с. $T_"download" = 15000 slash 200 approx 75$~с.
- *Peer cache, $k = 1$*: $B_"local" = 1250$~МБ/с (10~Гбит/с). $T_"download" = 15000 slash 1250 approx 12$~с --- ускорение в $approx 6.3$ раза.
- *Local cache*: $B_"local" = 7000$~МБ/с (NVMe). $T_"download" = 15000 slash 7000 approx 2.1$~с --- ускорение в $approx 36$ раз.

Таким образом, попадание в локальный кэш даёт наибольший выигрыш. При отсутствии локального кэша один пир обеспечивает ускорение в 6~раз по сравнению с загрузкой из внешнего репозитория.

*Закон Амдала и предел параллельного ускорения*

Параллельная загрузка чанков с нескольких пиров ограничена законом Амдала @amdahl_law:

$ S(k) = 1 / (alpha + (1 - alpha) / k) $

где $alpha$ --- доля последовательной части (Redis lookup, инициализация соединений), $k$ --- число параллельных пиров. При $alpha = 0.05$ и $k = 5$: $S(5) = 1 \/ (0.05 + 0.19) approx 4.2$. Практически оптимальным является диапазон $k = 2$--$4$ при балансе между параллелизмом и overhead на координацию.

*Применение закона Литтла*

Закон Литтла @little1961 $L = lambda dot W$ позволяет оценить число одновременно ожидающих запросов. При интенсивности входящих запросов $lambda = 5$~запр./с:

- Baseline ($W = 75$~с): $L = 5 times 75 = 375$ ожидающих запросов.
- P2P/FUSE после прогрева ($W = 2.1$~с): $L = 5 times 2.1 approx 11$ ожидающих запросов.

Снижение числа ожидающих запросов в $approx 34$~раза непосредственно транслируется в снижение нагрузки на очередь и улучшение соблюдения SLA по задержке ответа.

== Ожидаемые результаты сравнения

#figure(
  table(
    columns: (1.5fr, 1fr, 1fr, 1.1fr),
    stroke: (x, y) => if y == 0 or y == 1 { (bottom: 0.5pt) } else { none },
    table.header(
      [*Показатель*], [*emptyDir*], [*NFS*], [*P2P/FUSE*],
    ),
    [$T_"model_ready"$, первый запуск],
    [$approx$75 с], [$approx$75 с], [$approx$75 с (fallback)],

    [$T_"model_ready"$, повтор на той же ноде],
    [$approx$75 с], [$approx$12 с], [$approx$2 с (local cache)],

    [$T_"model_ready"$, повтор на соседней ноде],
    [$approx$75 с], [$approx$12 с], [$approx$12 с (peer cache)],

    [$T_"first_byte"$],
    [$approx$75 с], [$approx$75 с], [$<$1 с (lazy FUSE)],

    [Внешний трафик, $N = 10$ реплик],
    [$10 times 15 = 150$ ГБ], [15 ГБ], [$lt.eq$15 ГБ],

    [SPOF],
    [Нет], [NFS-сервер], [Redis (с HA --- нет)],

    [Деградация при отказе],
    [Нет], [Полная], [Graceful (local+fallback)],

    [Сложность эксплуатации],
    [Низкая], [Средняя], [Средняя],
  ),
  caption: [Сравнение подходов по ключевым показателям],
) <tab-comparison>

Ключевым преимуществом P2P/FUSE-подхода является значение $T_"first_byte" < 1$~с: FUSE-монтирование позволяет runtime-контейнеру начать чтение сразу после запуска, тогда как физическая загрузка чанков происходит параллельно с чтением (lazy loading). Для baseline и NFS время до первого байта совпадает с полным временем загрузки, поскольку `storage-initializer` должен завершить скачивание до старта основного контейнера.

По показателю внешнего трафика P2P/FUSE при прогретом кластере эквивалентен NFS: загрузка происходит один раз. При этом данные распределяются по дискам всех нод, создавая избыточность без выделенного NFS-сервера.

#figure(
  image("chart_tmodel_ready.png", width: 88%),
  caption: [Время готовности модели ($T_"model_ready"$) по сценариям для Llama~3~8B (15~ГБ). При наличии локального кэша P2P/FUSE-подход обеспечивает $T_"model_ready" = 1.9$~с против 74.8~с у baseline.],
) <fig-tmodel-ready>

#figure(
  image("chart_external_traffic.png", width: 82%),
  caption: [Суммарный внешний трафик при масштабировании до $N$ реплик (Llama~3~8B, 15~ГБ). Подходы NFS и P2P/FUSE загружают модель однократно независимо от числа реплик; emptyDir генерирует $N times 15$~ГБ трафика.],
) <fig-external-traffic>

== Анализ сценариев отказов

*Падение worker-ноды с чанками*

При отказе ноды агент перестаёт обновлять TTL-записи в Redis. По истечении TTL (типично 30~с) записи удаляются. При запросе затронутого чанка агент соседней ноды получает пустой список пиров и инициирует fallback на HuggingFace. При репликации каждого чанка на $r gt.eq 2$ ноды вероятность потери доступности:

$ P_"unavail" = p^r $

где $p$ --- вероятность отказа одной ноды. При $p = 0.01$ и $r = 2$: $P_"unavail" = 10^(-4)$; при $r = 3$: $P_"unavail" = 10^(-6)$ @ford2010availability.

*Устаревшая запись в Redis (stale metadata)*

Агент пытается подключиться к указанному пиру, получает ошибку соединения, переходит к следующему пиру из множества `chunk:peers:{hash}` или инициирует fallback. Stale-записи автоматически удаляются по TTL, не требуя ручного вмешательства.

*Недоступность Redis*

При недоступности Redis агент теряет возможность находить пиров. Система деградирует до двухуровневой: local cache $arrow$ remote fallback. Обслуживание запросов продолжается при наличии локальных данных. При развёртывании Redis Sentinel (1 master, 2 replica) вероятность одновременного отказа всех узлов пренебрежимо мала при независимых отказах @redis_sentinel_docs.

*Недоступность HuggingFace Hub*

Система использует данные из local и peer cache. Новые модели, ещё не загруженные в кластер, становятся недоступны. Это является ожидаемой деградацией при условии предварительного прогрева популярных моделей. Возможным улучшением является поддержка зеркальных репозиториев в качестве альтернативных источников fallback.

*Частичная запись чанка*

При аварийном завершении агента в процессе записи чанка временный файл (с суффиксом `.tmp`) не переименовывается в финальный путь. При следующем запросе агент не найдёт чанк в локальном хранилище и повторит загрузку. Консистентность обеспечивается атомарностью системного вызова `os.Rename`, который переименовывает файл только при успешном завершении записи.

*Сетевое разбиение кластера*

При разбиении кластера на изолированные части агенты, не имеющие доступа к Redis master, переходят в degraded-режим: local cache + remote fallback. После восстановления сетевой связности агенты возобновляют heartbeat, и Redis автоматически обновляет реестр активных пиров.

#figure(
  image("chart_reliability.png", width: 78%),
  caption: [Деградация инференса при трёх сценариях отказов (прогретый кластер, $r = 3$). NFS полностью теряет доступ при отказе сервера или HF Hub; P2P/FUSE деградирует частично при недоступности Redis и остаётся полностью работоспособен при сбое HF Hub.],
) <fig-reliability>

== Ограничения предложенного подхода

*Накладные расходы FUSE.* Каждая операция чтения проходит через два context switch: пользовательское пространство → ядро (VFS-перехват) → пользовательское пространство (FUSE-демон). Для ML-workload с последовательными крупными чтениями (размер чанка 16~МБ) эти накладные расходы незначительны: при 2~context switch по $approx$5~мкс каждый и времени чтения чанка $approx$2.3~мс суммарные накладные расходы не превышают 0.5%. Для сценариев случайного доступа по малым диапазонам относительные накладные расходы возрастают.

*Redis как критический компонент.* Без HA-конфигурации Redis является SPOF для функции peer discovery. Данные при этом не теряются: система деградирует до local + fallback, сохраняя частичную работоспособность. HA-конфигурация (Sentinel или Cluster) устраняет эту единственную точку отказа на уровне сигнализации.

*Сложность наблюдаемости.* P2P-трафик менее прозрачен, чем чтение из NFS. Для корректного мониторинга требуется сбор метрик: счётчики local hit/miss, peer hit/miss, remote fallback; задержки по уровням; состояние Redis. Без инструментирования диагностика деградации производительности затруднена.

*Первый запуск в кластере.* При полном отсутствии кэша система вынуждена загружать модель из внешнего репозитория. Выигрыш появляется начиная со второго запуска. Возможным улучшением является предварительное прогревание кэша в периоды низкой нагрузки.

*Малые модели.* Для моделей размером до 1~ГБ накладные расходы на FUSE, Redis и P2P-координацию могут быть сравнимы с временем прямой загрузки через `emptyDir`. Архитектура наиболее эффективна для LLM и других крупных моделей ($>$5~ГБ).

*Безопасность.* В текущем прототипе TCP-соединения между агентами не аутентифицированы. В production-окружении необходима поддержка mTLS между агентами, авторизация запросов к Redis через ACL и шифрование трафика.

== Выводы по разделу

Расчётная модель AMAT и анализ формулы P2P-загрузки демонстрируют, что предложенная архитектура наиболее эффективна в сценариях повторных запусков и burst-масштабирования. При прогретом кластере (hit rate 90%) среднее время доступа к чанку снижается в 30~раз по сравнению с постоянным обращением к внешнему репозиторию, а полное время загрузки модели --- с 75~с до 2~с при попадании в локальный кэш. Механизм FUSE lazy loading обеспечивает время до первого байта менее 1~с, что принципиально недостижимо при `storage-initializer`-подходе. Анализ сценариев отказов показывает, что система деградирует gracefully: потеря worker-ноды затрагивает только нерезервированные чанки, а недоступность Redis переводит систему в двухуровневый режим с сохранением работоспособности.
