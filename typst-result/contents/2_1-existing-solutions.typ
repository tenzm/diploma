= АНАЛИЗ СУЩЕСТВУЮЩИХ РЕШЕНИЙ ДЛЯ ХРАНЕНИЯ И КЭШИРОВАНИЯ МОДЕЛЕЙ

В данном разделе исследуются архитектуры и свойства наиболее распространённых систем распределённого хранения и кэширования, рассматриваемых в качестве кандидатных решений для задачи ускорения холодного старта ML-инференса в Kubernetes. Для каждого решения проводится анализ применимости к сценарию бессерверного инференса. На основе анализа формулируется обоснование разработки специализированного решения.

== Критерии сравнения решений

Чтобы сравнение систем было обоснованным и воспроизводимым, необходимо зафиксировать критерии, непосредственно вытекающие из требований, сформулированных в разделе 1.

*Задержка при холодном старте* определяет, насколько система способна уменьшить время до первого доступного байта весов модели. Ключевой характеристикой здесь является поддержка ленивой загрузки (lazy loading) или чтения по требованию.

*Поддержка Kubernetes* означает наличие официального механизма развёртывания в контейнерной среде: CSI-драйвера, Helm-чарта или Kubernetes Operator.

*Локальный кэш на ноде* --- возможность хранить горячие данные непосредственно на дисках worker-нод вблизи compute-ресурсов, что обеспечивает минимальную latency при повторном чтении.

*P2P-доставка* отражает способность системы передавать данные между worker-нодами без обращения к центральному источнику, тем самым распределяя нагрузку и ускоряя одновременное масштабирование большого числа реплик инференса.

*FUSE/POSIX-интерфейс* оценивает, предоставляет ли система runtime-контейнеру стандартный файловый путь, не требующий изменений в ML-фреймворке.

*Отказоустойчивость* характеризует поведение системы при выходе из строя отдельных узлов: существуют ли механизм репликации данных и автоматический fallback на альтернативный источник.

*Наличие единой точки отказа (SPOF)* указывает, имеется ли в архитектуре компонент, отказ которого делает недоступными все данные.

*Зависимость от внешнего объектного хранилища* определяет, является ли object storage обязательным звеном в пути чтения.

*Применимость к serverless cold start* является интегральным критерием: насколько хорошо решение адаптировано к сценарию эфемерных подов, работающих с крупными бинарными файлами весов при жёстких ограничениях на время инициализации.

Перечисленные критерии используются в сравнительной таблице @tab-solutions-comparison и в итоговом обосновании разработки специализированного решения.

== Alluxio

Alluxio (ранее Tachyon) --- виртуальная распределённая файловая система (Virtual Distributed File System, VDFS), выступающая слоем оркестрации данных между вычислительными фреймворками и системами постоянного хранения (Amazon S3, HDFS, NFS и другими) @alluxio_arch. Система разработана в Калифорнийском университете в Беркли и нашла применение в крупномасштабных аналитических и ML-рабочих нагрузках.

*Архитектура.* Alluxio строится по модели Master/Worker/Client. _Ведущий узел_ (Leading Master) управляет глобальными метаданными: деревом inode, распределением блоков и ёмкостью рабочих узлов. Для обеспечения высокой доступности (HA) рядом с ведущим мастером работают несколько резервных мастеров. _Worker-узлы_ управляют локальными ресурсами хранения (RAM, SSD, HDD), выделенными под кэш Alluxio. При заполнении ёмкости применяются политики вытеснения (LRU и аналоги). _Client_ предоставляет приложению шлюз к кластеру Alluxio. Интеграция с Kubernetes реализована через Helm-чарты и Alluxio Operator @alluxio_k8s. Alluxio поддерживает монтирование через FUSE (Alluxio POSIX API), что обеспечивает прозрачный доступ для приложений.

*Преимущества.* Alluxio обеспечивает высокую производительность за счёт приоритетного кэширования в оперативной памяти (memory-first tiered architecture). Унифицированное пространство имён (Unified Namespace) позволяет монтировать разнородные хранилища в единую логическую файловую систему. Поддержка локальности данных выгодна для ML-нагрузок: когда модельные данные кэшируются на ноде, где запущен инференс, задержка чтения снижается до уровня локального диска.

*Недостатки.* Первый существенный недостаток --- _сложность эксплуатации_. Настройка HA-конфигурации мастеров, управление eviction-политиками, диагностика утечек памяти в Worker-узлах и оптимизация распределения блоков требуют глубоких знаний архитектуры системы. Операционная нагрузка несопоставима с задачей кэширования весов модели.

Второй недостаток --- _высокое потребление памяти_. Alluxio является memory-centric системой: для достижения декларируемой производительности Worker-узлы должны располагать значительным объёмом оперативной памяти. При кэшировании нескольких больших моделей (десятки гигабайт каждая) потребности в памяти конкурируют с самим GPU-инференсом.

Третий недостаток --- _модель лицензирования_. Ключевые функции для производственных ML-сред: децентрализованная архитектура, расширенные политики кэширования и продвинутые AI/ML-оптимизации --- доступны только в Enterprise Edition @alluxio_pricing.

Четвёртый недостаток --- _несовместимость с serverless-подами_. Клиент Alluxio предполагает наличие состояния и устойчивого соединения с кластером. В эфемерных подах Kubernetes каждая инициализация клиента добавляет задержку к холодному старту.

*Вывод.* Alluxio технически близок к задаче: он обеспечивает кэширование данных с локальностью и поддерживает FUSE-монтирование. Однако он представляет собой тяжёлый data orchestration layer общего назначения, предназначенный для аналитики и распределённого ML-обучения. Для узкой задачи chunk-кэширования весов модели в serverless-инференсе это архитектурно и операционно избыточное решение.

== JuiceFS

JuiceFS --- высокопроизводительная распределённая файловая система с POSIX-совместимым интерфейсом, ориентированная на облачные среды @juicefs_intro. Community Edition распространяется под лицензией Apache 2.0. Система нашла применение в ML-платформах благодаря нативной интеграции с Kubernetes и возможности использовать уже существующие объектные хранилища как backend.

*Архитектура.* Фундаментальной особенностью JuiceFS является _строгое разделение данных и метаданных_. Архитектура включает три компонента.

_Metadata Engine_ хранит всю информацию о файловой системе: имена файлов, права доступа, временны́е метки, структуру директорий и отображение файлов на физические блоки данных. Community Edition поддерживает несколько бэкендов: Redis, TiKV, PostgreSQL, MySQL/MariaDB @juicefs_design.

_Object Storage_ хранит содержимое файлов в виде блоков. При записи файл логически делится на чанки по 64 МБ, каждый из которых при сбросе разбивается на блоки по 4 МБ, параллельно загружаемые в объектное хранилище @juicefs_design.

_Клиент JuiceFS_ реализует файловый интерфейс (FUSE, CSI-драйвер, S3 Gateway) и взаимодействует одновременно с Metadata Engine и с объектным хранилищем.

В Kubernetes JuiceFS развёртывается через CSI-драйвер, который реализует архитектуру с выделенным Mount Pod на каждом узле. Кэширование на локальных дисках является ключевым механизмом ускорения: при повторном чтении одних и тех же блоков JuiceFS обслуживает запросы с локального NVMe-диска вместо обращения к объектному хранилищу @juicefs_perf.

*Ограничения для serverless cold start.*

_Зависимость от объектного хранилища._ JuiceFS не имеет собственного слоя хранения данных и обязательно требует внешнего S3-совместимого хранилища. При промахе кэша (cache miss) клиент обращается к объектному хранилищу, задержки которого на порядок превышают задержки локального диска.

_Metadata Engine как потенциальная точка отказа._ При использовании Redis в качестве бэкенда метаданных и отсутствии HA-конфигурации отказ экземпляра Redis делает недоступной всю файловую систему.

_Проблема прогрева кэша в эфемерных средах._ В serverless-сценарии поды часто запускаются на «чистых» узлах, где локальный кэш пуст. При масштабировании на новые узлы все читаемые блоки будут приходить из объектного хранилища --- именно в тот момент, когда критична latency первого байта.

*Вывод.* JuiceFS представляет ценный архитектурный ориентир: разделение данных и метаданных, использование Redis как быстрого metadata backend, FUSE-интерфейс --- все эти идеи применены в разрабатываемой системе. Однако JuiceFS является файловой системой общего назначения с обязательной зависимостью от объектного хранилища, тогда как для решения задачи требуется специализированный cache/fetch слой с P2P-передачей чанков между нодами кластера.

== Dragonfly, CubeFS и Rook/Ceph

*Dragonfly.* Dragonfly --- P2P-система распределения файлов и контейнерных образов, разработанная Alibaba Cloud и переданная в CNCF @dragonfly_blog. Система ориентирована на сценарии эффективного распространения крупных файлов на большое число узлов одновременно.

_Архитектура._ Dragonfly сочетает элементы CDN и P2P. _Scheduler_ является центральным компонентом: он отслеживает доступность узлов-пиров и определяет оптимальные источники для каждого запроса. _Seed Peer_ --- специализированный пир, кэширующий данные и выступающий первичным источником для других пиров. _Peer (dfdaemon)_ работает на каждом узле Kubernetes как DaemonSet.

_Применимость к ML-инференсу._ Dragonfly хорошо зарекомендовал себя для параллельной дистрибуции образов контейнеров на сотни нод. Та же модель применима к весам ML-моделей.

_Ограничения._ Dragonfly не предоставляет стандартного FUSE/POSIX-интерфейса: система занимается доставкой файлов, но не монтированием файловой системы. Для предоставления ML-runtime доступа к весам через обычный файловый путь потребуется дополнительный уровень. Наличие централизованного Scheduler вводит компонент, отказ которого нарушает координацию P2P-сети.

*CubeFS.* CubeFS --- облачно-ориентированная распределённая файловая система уровня CNCF Graduated @cubefs_arch, разработанная JD.com. _Архитектура_ включает: Master-узлы (управление ресурсами кластера через Raft), Meta Nodes (управление шардами метаданных), Data Nodes (управление шардами данных с репликацией). CubeFS поддерживает POSIX, S3 и HDFS-интерфейсы, а также CSI-драйвер для Kubernetes.

_Ограничения._ Сложность развёртывания CubeFS сопоставима с Ceph: кластер требует управления несколькими типами узлов, настройки репликации, мониторинга состояния и обеспечения кворума Meta Nodes. Кроме того, в CubeFS отсутствуют P2P-доставка чанков и встроенный механизм lazy loading для ML-весов.

*Rook/Ceph.* Rook --- cloud-native оркестратор для Kubernetes, автоматизирующий развёртывание и управление распределёнными системами хранения @rook_docs. В наиболее распространённой конфигурации Rook используется для оркестрации Ceph.

_Архитектура Ceph_ @ceph_arch. Ceph предоставляет блочное (RADOS Block Device, RBD), файловое (CephFS) и объектное (RADOS Gateway, RGW) хранилище в единой системе. Архитектура строится на: Monitor (MON) --- поддерживает cluster map через алгоритм Paxos; Manager (MGR) --- мониторинг и управление; OSD Daemon --- хранит данные на физических дисках; Metadata Server (MDS) --- управляет метаданными CephFS.

_Ограничения._ Развёртывание Ceph требует специализированных знаний. Минимальный production-кластер требует нескольких выделенных узлов хранения с отдельными дисками. Ceph является универсальной production-системой хранения, а не специализированным решением для кэширования весов ML-моделей.

== Сравнительная таблица

Для удобства сопоставления результаты анализа сведены в таблицу @tab-solutions-comparison.

#figure(
  table(
    columns: 6,
    stroke: (x, y) => if y == 0 or y == 1 { (bottom: 0.5pt) } else { none },
    table.header(
      [*Критерий*], [*Alluxio*], [*JuiceFS*], [*Dragonfly*], [*CubeFS*], [*Rook/Ceph*],
    ),
    [Лок. кэш на ноде],         [Да (RAM/SSD)],    [Да (диск)],     [Да (диск)],      [Да (диск)],     [Да (OSD)],
    [P2P-доставка],              [Нет],              [Нет],           [Да],             [Нет],           [Нет],
    [FUSE/POSIX],                [Частично],         [Да],            [Нет],            [Да],            [Да (CephFS)],
    [K8s-интеграция],            [Helm/Operator],    [CSI],           [DaemonSet],      [CSI],           [Rook Operator],
    [SPOF в архитектуре],        [Master (OSS)],     [Metadata Eng.], [Scheduler],      [Master],        [MON (кворум)],
    [Объектное хранилище],       [Опционально],      [Обязательно],   [Не нужно],       [Не нужно],      [Встроено (RGW)],
    [Сложность эксплуатации],    [Высокая],          [Средняя],       [Средняя],        [Высокая],       [Очень высокая],
    [Применимость к serverless], [Средняя],          [Низкая],        [Высокая],        [Средняя],       [Низкая],
    [Лицензия],                  [Community/Ent.],   [Apache 2.0],    [Apache 2.0],     [Apache 2.0],    [LGPL/Apache],
  ),
  caption: [Сравнение решений для хранения и кэширования ML-моделей],
) <tab-solutions-comparison>

Анализ таблицы @tab-solutions-comparison позволяет сформулировать следующие наблюдения. Ни одно из рассмотренных решений не удовлетворяет всем требованиям одновременно. Dragonfly наиболее близок по концепции P2P-доставки и показывает высокую применимость к serverless-сценариям, однако не предоставляет FUSE-интерфейса. JuiceFS имеет POSIX-интерфейс и хорошую архитектуру метаданных, но обязательная зависимость от объектного хранилища и отсутствие P2P между нодами делают его неоптимальным для данной задачи.

== Обоснование разработки специализированного решения

Проведённый анализ демонстрирует системное несоответствие между возможностями существующих решений и требованиями задачи.

*Специализация вместо универсальности.* Все рассмотренные системы обеспечивают функциональность, существенно выходящую за рамки необходимой. Для задачи serverless-инференса нужен узкий cache/fetch слой, ориентированный на оптимизацию чтения крупных бинарных файлов по фрагментам.

*Приоритет локальности данных.* Ни одна из рассмотренных систем не реализует явного приоритета «локальный диск → соседняя нода → внешний источник» в виде встроенного трёхуровневого read path для serverless-сценария.

*P2P в сочетании с FUSE.* Dragonfly реализует P2P-доставку, но не предоставляет POSIX-интерфейса. JuiceFS предоставляет FUSE, но не поддерживает P2P между нодами. Требуемая комбинация отсутствует ни в одном из существующих решений в готовом виде.

*Простая модель метаданных с TTL.* Redis в роли реестра метаданных чанков обеспечивает атомарные операции, низкую задержку lookup ($O(1)$ по ключу) и встроенный механизм TTL. Это принципиально проще, чем поддержка полноценного Metadata Engine с журналами, репликой Raft и сложной схемой партиционирования.

*Fallback без жёсткой зависимости.* Специализированная система допускает ситуацию, когда нужный чанк отсутствует в кластере: в этом случае он скачивается из внешнего источника и сохраняется в локальном кэше. Это устраняет принципиальную зависимость от объектного хранилища как обязательного звена в пути чтения.

*Отказоустойчивость без центрального файлового сервера.* Специализированная система использует локальные диски всех worker-нод как распределённый кэш: при отказе одной ноды чанки, хранившиеся на ней, могут быть получены от других нод или из внешнего источника. Redis с TTL автоматически удаляет устаревшие записи об отказавшем агенте.

Дополнительным аргументом является экономическая доступность: все описанные компоненты (Redis, FUSE-библиотека, Go-реализация агента) распространяются под открытыми лицензиями.

== Выводы по разделу

Рассмотренные решения --- Alluxio, JuiceFS, Dragonfly, CubeFS и Rook/Ceph --- предоставляют ценные архитектурные идеи, нашедшие отражение в разрабатываемой системе: разделение данных и метаданных (JuiceFS), P2P-дистрибуция между узлами кластера (Dragonfly), предоставление POSIX-совместимого интерфейса через FUSE (JuiceFS, CubeFS, CephFS).

Вместе с тем ни одно из существующих решений не обеспечивает требуемую комбинацию свойств: трёхуровневый read path с приоритетом локального кэша, P2P-доставку чанков между нодами и прозрачный FUSE-интерфейс для ML-runtime без обязательной зависимости от внешнего объектного хранилища и тяжёлой операционной инфраструктуры.

Это обосновывает разработку специализированной системы: lightweight chunk-кэша на локальных дисках worker-нод, сопровождаемого Redis-реестром метаданных чанков и FUSE-клиентом, обеспечивающим runtime-контейнеру стандартный файловый интерфейс.
