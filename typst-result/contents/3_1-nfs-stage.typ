= ПРОМЕЖУТОЧНОЕ РЕШЕНИЕ НА БАЗЕ NFS И КООРДИНАЦИИ ЗАГРУЗОК

В данном разделе описывается промежуточный этап оптимизации системы бессерверного инференса: переход от изолированной загрузки весов модели в том `emptyDir` к использованию сетевой файловой системы NFS в качестве общего хранилища. Раскрываются недостатки исходной схемы, механизм интеграции NFS в Kubernetes, проблемы синхронизации параллельных загрузок, особенности файловых блокировок поверх сетевых файловых систем, применение механизма Leader Election через Kubernetes Lease API, а также системные ограничения, обусловившие необходимость разработки распределённой P2P/FUSE-архитектуры.

== Исходная схема: storage-initializer и emptyDir

*Паттерн init-контейнера и жизненный цикл тома emptyDir*

Исходная схема развёртывания модели в Kubernetes использует паттерн init-контейнера. Под (pod) содержит два контейнера: `storage-initializer`, запускаемый первым в режиме init, и runtime-контейнер. Init-контейнер монтирует том типа `emptyDir`, запускает процесс загрузки весов модели из внешнего репозитория и записывает файлы в смонтированный том. После успешного завершения init-контейнера запускается runtime-контейнер (vLLM, TGI, Triton), который читает веса из того же тома `emptyDir` и загружает их в GPU-память.

Том типа `emptyDir` создаётся Kubernetes в момент планирования пода на узел и физически размещается на локальном диске этого узла. Его жизненный цикл жёстко привязан к жизненному циклу пода: при удалении или перепланировании пода том уничтожается вместе со всем содержимым.

*Фундаментальный недостаток: повторные загрузки*

Отсутствие разделяемого хранилища порождает фундаментальный недостаток данной схемы. При создании новой реплики, даже на той же узловой машине, каждый под выполняет полную загрузку файлов модели независимо от всех ранее запущенных подов.

При масштабировании до $N$ реплик внешний репозиторий получает $N$ независимых последовательных или параллельных запросов на один и тот же набор файлов. Для современных моделей класса LLM с объёмом весов от 7 до 70 ГБ это означает:

- суммарный исходящий трафик на уровне десятков и сотен гигабайт при каждом событии масштабирования;
- время cold start каждой реплики полностью определяется скоростью загрузки из внешнего источника;
- нагрузка на внешний канал связи возрастает пропорционально числу реплик, что может приводить к деградации пропускной способности и превышению rate-лимитов репозитория.

== Переход к NFS как общему хранилищу

*NFS и режим ReadWriteMany*

Network File System (NFS) --- зрелый протокол сетевого доступа к файлам, стандартизированный в версиях NFSv3 (RFC 1813) и NFSv4 (RFC 7530). В контексте Kubernetes NFS привлекателен прежде всего тем, что поддерживает режим доступа `ReadWriteMany` (RWX), при котором несколько подов на разных узлах кластера могут одновременно монтировать один и тот же том @nfs_k8s_docs.

*Интеграция в Kubernetes: PV и PVC*

Схема использования NFS в Kubernetes предполагает создание двух ресурсов: `PersistentVolume` (PV), описывающего физическое хранилище, и `PersistentVolumeClaim` (PVC), представляющего запрос пода на использование этого хранилища.

```yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: models-nfs-pv
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  nfs:
    path: /exports/models
    server: 10.0.1.100
```

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models-nfs-pvc
  namespace: inference
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  volumeName: models-nfs-pv
```

Введение NFS принципиально изменяет характер загрузок: первый под, обнаруживший отсутствие модели в NFS-директории, загружает её из внешнего репозитория и записывает в общее хранилище. Все последующие поды обнаруживают файлы уже готовыми и пропускают этап загрузки. Таким образом, при масштабировании с 1 до $N$ реплик загрузка из внешнего источника выполняется ровно один раз.

== Проблема параллельного скачивания и условие гонки

*Race condition при первом развёртывании*

Несмотря на устранение повторных загрузок при масштабировании уже существующей модели, переход к NFS порождает новую задачу: состояние гонки (race condition) при первом развёртывании. В этом сценарии несколько реплик запускаются одновременно, каждая проверяет директорию модели в NFS, обнаруживает её отсутствие и независимо начинает параллельную загрузку весов по одному и тому же пути назначения.

Параллельная запись одних и тех же файлов из нескольких источников приводит к непредсказуемым последствиям: файлы могут оказаться повреждёнными вследствие чередования блоков от разных источников, директория модели может находиться в частично записанном состоянии.

*Алгоритм координированной загрузки*

Для решения задачи консистентности разработан алгоритм, реализующий паттерн «один загрузчик --- множество ожидающих»:

+ Под проверяет наличие финального маркер-файла `.ready` в директории модели. Если маркер присутствует, загрузка не требуется.
+ Если маркер отсутствует, под открывает файл блокировки `.lock` и выполняет попытку получения эксклюзивной блокировки (`LOCK_EX | LOCK_NB`).
+ Под, успешно получивший блокировку, становится лидером загрузки. Лидер повторно проверяет наличие маркера `.ready`.
+ Лидер начинает загрузку весов модели во временную директорию `<model>_tmp`.
+ После завершения загрузки лидер выполняет атомарное переименование: `<model>_tmp → <model>`.
+ Лидер создаёт маркер-файл `.ready` и освобождает файловую блокировку.

Поды, не получившие эксклюзивную блокировку, устанавливают разделяемую блокировку (`LOCK_SH`) и ожидают. После возврата из блокирующего вызова под повторно проверяет маркер `.ready`.

Алгоритм в псевдокоде:

```python
function EnsureModelReady(model_dir, lock_path, ready_marker):
    if Exists(ready_marker):
        return OK

    lock_fd = Open(lock_path, CREATE | RDWR)

    if TryLock(lock_fd, LOCK_EX | LOCK_NB) == SUCCESS:
        if Exists(ready_marker):
            Unlock(lock_fd)
            return OK

        tmp_dir = model_dir + "_tmp"
        err = DownloadModel(source_url, tmp_dir)
        if err != nil:
            Unlock(lock_fd)
            RemoveAll(tmp_dir)
            return ERROR

        Rename(tmp_dir, model_dir)   # атомарно
        CreateFile(ready_marker)
        Unlock(lock_fd)
        return OK
    else:
        Lock(lock_fd, LOCK_SH)       # блокирующий вызов
        Unlock(lock_fd)
        return EnsureModelReady(model_dir, lock_path, ready_marker)
```

== Файловые блокировки flock и их особенности поверх NFS

*Advisory locking: разделяемые и эксклюзивные блокировки*

Системный вызов `flock(2)` реализует рекомендательные (advisory) блокировки на уровне открытого файлового дескриптора @flock_man. Рекомендательный характер означает, что ядро операционной системы не препятствует доступу к файлу процессам, которые не запрашивают блокировку. Консистентность обеспечивается только при условии, что все участники явно соблюдают соглашение о блокировках.

Поддерживаются два вида блокировок:

- *Разделяемая* (`LOCK_SH`) --- несколько процессов могут одновременно удерживать разделяемую блокировку на одном файле.
- *Эксклюзивная* (`LOCK_EX`) --- только один процесс может удерживать эксклюзивную блокировку. Пока она активна, ни один другой процесс не может получить ни разделяемую, ни эксклюзивную блокировку.

Блокировки `flock` автоматически снимаются при завершении процесса, что является встроенной защитой от «зависших» блокировок.

*NFSv3: блокировки через Network Lock Manager*

В NFSv3 блокировки файлов реализуются через вспомогательный сервис Network Lock Manager (NLM). Данная архитектура имеет ряд ограничений: при сетевом разделе блокировка может зависнуть; при перезагрузке NFS-сервера состояние блокировок восстанавливается не всегда корректно.

*NFSv4: встроенные блокировки*

В NFSv4 файловые блокировки интегрированы непосредственно в основной протокол. Сервер NFSv4 отслеживает состояние всех активных блокировок через механизм аренды (lease): каждый клиент обязан периодически подтверждать свою активность; при истечении аренды сервер освобождает все блокировки данного клиента.

*Практическая модель надёжности: flock + rename + маркер*

Описанный алгоритм координированной загрузки использует комбинацию трёх механизмов, образующих эшелонированную защиту:

+ *Файловая блокировка* (`flock`) --- координирует конкурентный доступ в штатном режиме работы.
+ *Атомарное переименование* (`rename`) --- гарантирует, что читатели никогда не видят частично записанных данных. Операция `rename(2)` атомарна в пределах одной файловой системы.
+ *Маркер готовности* (`.ready`) --- является финальным критерием готовности модели. Даже если блокировка была выдана некорректно двум клиентам одновременно, только тот под завершит загрузку, который успеет выполнить `rename` и создать маркер первым.

== Kubernetes Lease и Leader Election

*Ресурс coordination.k8s.io/v1/Lease*

Файловые блокировки поверх NFS остаются уязвимыми к ситуациям, связанным с некорректной конфигурацией NFS-клиента. Для случаев, когда требуется более высокий уровень гарантий, Kubernetes предоставляет встроенный механизм распределённой координации через ресурс `coordination.k8s.io/v1/Lease` @k8s_lease_api.

Ресурс `Lease` хранится в etcd и обладает строгими гарантиями согласованности за счёт оптимистичной блокировки через поле `resourceVersion`. Объект содержит ключевые поля:

- `holderIdentity` --- строковый идентификатор текущего держателя аренды.
- `leaseDurationSeconds` --- максимальная длительность аренды без обновления.
- `renewTime` --- временна́я метка последнего обновления аренды текущим держателем.
- `acquireTime` --- временна́я метка первоначального захвата аренды.
- `leaderTransitions` --- счётчик количества смен лидера.

Kubernetes API-сервер гарантирует, что при одновременной попытке обновить объект `Lease` несколькими клиентами только один запрос будет успешным: операция обновления проверяет `resourceVersion`.

*Алгоритм client-go leaderelection*

Стандартная библиотека `k8s.io/client-go/tools/leaderelection` реализует алгоритм выбора лидера поверх ресурса `Lease`:

+ При запуске каждый кандидат пытается создать объект `Lease` с собственным `holderIdentity` или обновить существующий объект, если аренда истекла.
+ Кандидат, чей запрос был принят первым, становится лидером. Остальные переходят в режим ожидания.
+ Лидер периодически обновляет поле `renewTime`, не давая сроку аренды истечь.
+ Если лидер завершается аварийно, обновление прекращается. По истечении `leaseDurationSeconds` аренда помечается как просроченная.
+ Один из ожидающих кандидатов захватывает `Lease` и становится новым лидером.

В контексте загрузки моделей ресурс `Lease` именуется по схеме `model-<name>-<revision>`, что позволяет независимо координировать загрузку разных моделей. Преимущество Kubernetes Lease перед `flock` состоит в том, что гарантии согласованности обеспечиваются etcd --- распределённым хранилищем с алгоритмом Raft, а не файловой системой.

== Ограничения NFS-этапа

*Централизованный сервер как единая точка отказа*

Несмотря на существенное улучшение по сравнению со схемой `emptyDir`, NFS-подход обнаружил ряд системных ограничений.

*NFS-сервер как SPOF.* Весь ввод-вывод чтения весов моделей проходит через один NFS-сервер. При его недоступности --- перезагрузке, сетевой проблеме, исчерпании дискового пространства --- вся система инференса лишается доступа к моделям.

*Bottleneck пропускной способности.* При одновременном масштабировании большого числа реплик все они читают файлы модели через сетевую карту NFS-сервера. Пропускная способность его сетевого интерфейса физически ограничена и не масштабируется горизонтально.

*Отсутствие локальности данных.* Даже если узел уже обслуживал данную модель ранее, при создании нового пода runtime-контейнер читает данные из NFS по сети заново. Локальный диск узла не используется для кэширования.

*Нагрузка на сеть при burst-масштабировании.* В отличие от схемы `emptyDir`, где нагрузка распределялась между нодами, в NFS-схеме весь трафик концентрируется на одном сервере.

*CAP-теорема и выбор NFS*

CAP-теорема @brewer2000cap утверждает, что распределённая система не может одновременно обеспечивать согласованность (Consistency), доступность (Availability) и устойчивость к разделению сети (Partition tolerance). Классический NFS с режимом монтирования `hard` выбирает согласованность, жертвуя доступностью: при недоступности сервера все операции ввода-вывода блокируются.

В итоге NFS-этап предлагает приемлемые характеристики для небольших кластеров с умеренной нагрузкой, но системно неприменим в условиях масштабирования, требующего высокой параллельности и устойчивости к сбоям сети.

== Выводы по разделу

NFS-этап значительно сократил количество обращений к внешнему репозиторию: вместо $N$ независимых загрузок на $N$ реплик однократная загрузка в общее хранилище обеспечивает повторное использование данных всеми подами. Алгоритм координации через файловые блокировки `flock` и маркер готовности обеспечивает консистентность при параллельном старте. Kubernetes Lease API предоставляет более надёжную альтернативу для координации в случаях, когда гарантии NFS-блокировок недостаточны.

Вместе с тем NFS как централизованное сетевое хранилище переносит проблему с внешнего репозитория на один внутренний сервер, создавая единую точку отказа и ограниченный по пропускной способности ресурс. Отсутствие локального кэширования на дисках нод означает, что одни и те же данные многократно передаются по сети при каждом событии масштабирования.

Для устранения этих ограничений необходим переход к архитектуре, в которой данные хранятся на локальных дисках worker-нод и доставляются напрямую между агентами, минуя централизованный сервер. Такая архитектура составляет содержание следующего раздела.
