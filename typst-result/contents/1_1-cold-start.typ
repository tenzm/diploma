= ПРОБЛЕМА ХОЛОДНОГО СТАРТА ML-МОДЕЛЕЙ В БЕССЕРВЕРНОМ ИНФЕРЕНСЕ

Настоящий раздел посвящён обоснованию актуальности проблемы холодного старта в системах бессерверного инференса моделей машинного обучения. Рассматривается эволюция архитектур и размеров ML-моделей, показывающая, что объём весовых файлов вырос от единиц мегабайт до сотен гигабайт. Раскрывается механизм каждого этапа холодного старта в Kubernetes, обосновывается, почему классическая схема с init-контейнером и томом `emptyDir` неэффективно использует уже загруженные данные. Оцениваются последствия для соблюдения SLA и пользовательского опыта с применением закона Литтла и вероятностной модели холодного старта. Формулируются функциональные и нефункциональные требования к системе оптимизации.

== Эволюция размеров моделей машинного обучения

=== От компактных сетей к масштабным языковым моделям

На раннем этапе развития глубокого обучения доминировали сверточные нейронные сети (CNN), ориентированные преимущественно на задачи компьютерного зрения. Прорывная работа Крижевского, Суцкевера и Хинтона представила модель AlexNet в 2012 году: сеть насчитывала 60 миллионов параметров, а объем её весовых файлов в 32-битном формате составлял около 233 МБ @alexnet2012. При типичной для того времени пропускной способности сети порядка 100 Мбит/с такой объём передавался за несколько секунд и не создавал значимых инфраструктурных проблем.

Качественный переход произошел в 2017 году с публикацией архитектуры Transformer. Механизм самовнимания (self-attention) обеспечил возможность эффективно обрабатывать длинные контекстные зависимости и параллелизовать обучение, что сделало Transformer доминирующей архитектурой для задач обработки естественного языка @bert2018. Вместе с этим значительно выросли и вычислительные требования.

Модель BERT Base (2018) с 110 миллионами параметров занимала около 440 МБ в 32-битном формате; версия BERT Large содержала 340 миллионов параметров и требовала порядка 1.4 ГБ @bert2018. Уже на этом этапе загрузка весов из удалённого хранилища начинала ощутимо влиять на задержку инициализации сервиса.

=== Экспоненциальный рост весов языковых моделей

Выход GPT-3 в 2020 году ознаменовал качественный скачок: 175 миллиардов параметров потребовали около 350 ГБ дискового пространства для хранения весов в 16-битном формате @gpt3_2020. Для загрузки такого объёма по каналу 10 Гбит/с требуется около 280 секунд --- почти пять минут только на передачу данных.

Тенденция к открытым моделям, начавшаяся с выпуска семейства Llama компании Meta, расширила доступ исследователей и производственных команд к крупным моделям. Llama 3 (2024) включает варианты с 8, 70 и 405 миллиардами параметров; модель Llama 3 70B в формате FP16 занимает около 148 ГБ @llamapaper2024.

#figure(
  table(
    columns: 6,
    stroke: (x, y) => if y == 0 or y == 1 { (bottom: 0.5pt) } else { none },
    table.header(
      [Модель], [Год], [Архитектура], [Параметры], [Вес (FP16)], [Влияние],
    ),
    [AlexNet],      [2012], [CNN],         [60 млн],    [$approx$0.1 ГБ], [Незначительное],
    [BERT Base],    [2018], [Transformer], [110 млн],   [$approx$0.2 ГБ], [Малое],
    [BERT Large],   [2018], [Transformer], [340 млн],   [$approx$0.7 ГБ], [Заметное],
    [GPT-2 Large],  [2019], [Transformer], [1.5 млрд],  [$approx$3 ГБ],   [Умеренное],
    [GPT-3],        [2020], [Transformer], [175 млрд],  [$approx$350 ГБ], [Критическое],
    [Llama 2 70B],  [2023], [Transformer], [70 млрд],   [$approx$140 ГБ], [Критическое],
    [Falcon 180B],  [2023], [Transformer], [180 млрд],  [$approx$360 ГБ], [Критическое],
    [Llama 3 70B],  [2024], [Transformer], [70 млрд],   [$approx$148 ГБ], [Критическое],
  ),
  caption: [Эволюция размеров ключевых ML-моделей],
) <tab-model-sizes>

Таблица @tab-model-sizes демонстрирует, что за период с 2018 по 2024 год размер весов ведущих производственных моделей вырос от сотен мегабайт до сотен гигабайт --- на три-четыре порядка. Если для BERT Base загрузка занимала секунды, то для Llama 3 70B при той же пропускной способности сети --- десятки минут. Именно это изменение масштаба превращает загрузку весов из тривиальной операции в доминирующий компонент задержки холодного старта.

#figure(
  image("chart_model_sizes.png", width: 88%),
  caption: [Рост размеров весов ключевых ML-моделей, 2019--2024 (логарифмическая шкала). За пять лет размер весов вырос более чем в 40 000 раз --- с 3 ГБ (GPT-2) до 360 ГБ (Falcon 180B).],
) <fig-model-sizes>

=== Законы масштабирования как движущая сила роста

Рост числа параметров подчиняется так называемым законам масштабирования (scaling laws). Эмпирически установлено, что качество языковых моделей, измеряемое кросс-энтропийными потерями, улучшается в степенной зависимости от трёх факторов: объёма параметров модели, размера обучающего набора данных и доступных вычислительных ресурсов @gpt3_2020. Из этих законов следует устойчивый технологический стимул к дальнейшему увеличению масштаба: большие модели достигают того же уровня качества с меньшим числом шагов обучения и могут демонстрировать способности, не наблюдаемые у меньших моделей.

Параллельно с ростом параметров развивается концепция «плотности возможностей» (capability density) @capability_density: компактные модели, такие как Mistral 7B и семейство Phi-3, достигают результатов, сопоставимых с более крупными аналогами, благодаря качественным обучающим данным и архитектурным оптимизациям. Однако даже модели в диапазоне 7--70 млрд параметров требуют от 14 до 148 ГБ дискового пространства для хранения весов --- объёмов, принципиально меняющих характер инфраструктурной задачи.

Таким образом, рост размеров ML-моделей является устойчивой долгосрочной тенденцией, а значит, проблема загрузки весов при холодном старте будет лишь обостряться с течением времени.

== Механизм холодного старта в Kubernetes

=== Декомпозиция задержки холодного старта

При развёртывании ML-инференса в Kubernetes каждый новый инстанс сервиса проходит ряд последовательных фаз. Суммарная задержка холодного старта формализуется следующим образом:

$ T_"start" = T_"schedule" + T_"pull" + T_"unpack" + T_"init" + T_"download" + T_"load", $ <eq-cold-start>

где $T_"schedule"$ --- время планировщика Kubernetes для назначения пода на конкретную ноду; $T_"pull"$ --- загрузка образа контейнера из реестра; $T_"unpack"$ --- декомпрессия и распаковка слоёв образа на диск; $T_"init"$ --- выполнение init-контейнера (в том числе storage-initializer); $T_"download"$ --- скачивание весов модели из внешнего репозитория; $T_"load"$ --- загрузка весов из файловой системы в оперативную память и GPU-видеопамять.

Каждое слагаемое вносит самостоятельный вклад в суммарную задержку.

=== Фаза планирования: $T_"schedule"$

Планировщик Kubernetes анализирует доступные ноды с учётом запрошенных ресурсов (CPU, память, GPU), ограничений через nodeSelector и tolerations, правил affinity и topologySpreadConstraints. При загруженном кластере или ожидании освобождения GPU-слота эта фаза может занимать от долей секунды до нескольких минут. Поскольку планировщик не учитывает наличие кэшированных весов модели на ноде, он может назначить под на ноду без нужных данных, вынуждая к их повторной загрузке.

=== Фазы загрузки и распаковки образа: $T_"pull" + T_"unpack"$

Образ контейнера для ML-инференса включает среду выполнения языка, фреймворк (PyTorch, TensorFlow), CUDA-библиотеки и вспомогательные зависимости. Для типичного контейнера с vLLM или Triton Inference Server размер образа может составлять 15--25 ГБ. Загрузка по каналу 10 Гбит/с занимает ориентировочно 12--20 секунд для передачи данных, однако реальное время больше из-за последовательного характера протокола реестра контейнеров @bentoml2024cold.

После скачивания каждый слой образа декомпрессируется из формата gzip или zstd и записывается на диск. Эта операция CPU-интенсивна: декомпрессия 20 ГБ образа может занять от трёх до восьми минут. Механизм Copy-on-Write, используемый большинством драйверов хранения контейнеров (overlay2, fuse-overlayfs), добавляет накладные расходы при последующем доступе к файлам внутри образа.

=== Фаза init-контейнера: $T_"init"$

Классическая схема развёртывания ML-инференса в Kubernetes предполагает использование init-контейнера (storage-initializer), который запускается до основного runtime-контейнера. Его задача --- скачать файлы весов модели из внешнего хранилища (HuggingFace Hub, S3, GCS) и записать их в том типа `emptyDir`, разделяемый с основным контейнером.

Принципиальная схема взаимодействия выглядит следующим образом: планировщик назначает под на ноду, после чего инициализируется том `emptyDir` на локальном диске этой ноды. Затем запускается init-контейнер `storage-initializer`, который последовательно скачивает файлы весов и записывает их в смонтированный `emptyDir`. После успешного завершения init-контейнера запускается runtime-контейнер (vLLM, TGI, Triton), который считывает файлы из `emptyDir` и загружает их в GPU-память.

=== Фаза загрузки весов из репозитория: $T_"download"$

Компонент $T_"download"$ является наиболее критичным с точки зрения зависимости от размера модели. При линейном росте объёма весов это слагаемое растёт пропорционально. Для Llama 3 8B (около 16 ГБ) при канале 1 Гбит/с без конкуренции загрузка занимает ориентировочно 128 секунд; для Llama 3 70B (около 148 ГБ) --- уже порядка 1190 секунд, то есть около 20 минут @bentoml2024cold.

Ключевая особенность проблемы состоит в том, что при масштабировании до $N$ реплик на разных нодах внешний репозиторий получает $N$ независимых запросов на один и тот же набор файлов. Каждый init-контейнер выполняет полную загрузку, игнорируя тот факт, что те же данные могут уже присутствовать на соседних нодах кластера.

=== Ограничения схемы с `emptyDir`

Том `emptyDir` привязан к жизненному циклу пода и физически размещается на диске конкретной ноды. При завершении или перепланировании пода данные в `emptyDir` безвозвратно удаляются. Следствием является отсутствие механизма разделения данных между репликами на одной ноде: при масштабировании к двум репликам на одном узле каждая из них независимо скачивает полный набор весов. Отсутствует и возможность передачи уже загруженных данных между нодами кластера, что исключает локальное повторное использование при перепланировании подов.

=== Фаза загрузки в GPU-память: $T_"load"$

После того как файлы весов оказались в `emptyDir`, runtime-контейнер (vLLM, TGI, Triton) считывает их с диска и загружает в GPU-память. Для Llama 3 8B эта операция занимает ориентировочно 60--120 секунд; для крупных моделей --- несколько минут. Кроме того, некоторые inference-движки, в частности vLLM, выполняют компиляцию графа CUDA и профилирование KV-кэша, что дополнительно увеличивает $T_"load"$ @bentoml2024cold.

=== Итоговый характер задержки

Для типичного контейнера с моделью Llama 3.1 8B суммарная задержка холодного старта на чистом узле складывается ориентировочно следующим образом: $T_"pull" + T_"unpack" approx 5$--8 мин; $T_"download" approx 2$--5 мин; $T_"load" approx 1$--3 мин. Итоговое время в зависимости от конфигурации сети и дисков может превышать 10--15 минут @bentoml2024cold.

При этом задержка масштабируется нелинейно: при переходе от 8B к 70B параметров $T_"download"$ возрастает пропорционально размеру весов, тогда как $T_"load"$ дополнительно зависит от скорости шины памяти GPU и конкретного inference-движка.

#figure(
  image("chart_cold_start_breakdown.png", width: 88%),
  caption: [Декомпозиция задержки холодного старта для Llama 3 8B (15 ГБ, канал 200 МБ/с). Фаза загрузки весов ($T_"download" approx 75$ с) составляет около 80% суммарного времени $approx 94$ с.],
) <fig-cold-start-breakdown>

== Влияние холодного старта на SLA, пользовательский опыт и стоимость

=== SLA и метрики задержки

Соглашение об уровне обслуживания (SLA) для интерактивных AI-сервисов, как правило, регламентирует задержку на перцентилях P95 и P99, а также специфические для генеративных моделей метрики: TTFT (Time to First Token --- время до первого токена) и TPS (Tokens Per Second --- скорость генерации). Для диалоговых ассистентов требования к TTFT на P95 нередко составляют от нескольких сотен миллисекунд до нескольких секунд, а для голосовых систем порог ещё ниже.

Холодный старт, занимающий 10--15 минут, делает соблюдение таких требований при использовании стратегии scale-to-zero принципиально невозможным без специальных архитектурных мер. Запросы, поступившие в момент инициализации пода, вынуждены ожидать завершения всей цепочки фаз из формулы @eq-cold-start, что многократно нарушает целевые значения P95 и P99 latency.

=== Закон Литтла: накопление ожидающих запросов

Для количественной оценки влияния холодного старта применим закон Литтла @little1961. Рассмотрим подсистему ожидания инициализации подов как стационарную систему массового обслуживания. Если $L_"wait"$ --- среднее число запросов, находящихся в состоянии ожидания завершения холодного старта, $lambda$ --- интенсивность входящих запросов в период масштабирования, а $W_"cold"$ --- среднее время холодного старта, то:

$ L_"wait" = lambda dot W_"cold". $ <eq-little>

Из формулы @eq-little следует фундаментальный вывод: при $lambda = 10$ запр./с и $W_"cold" = 600$ с (10 мин) в очереди единовременно накапливается $L_"wait" = 6000$ ожидающих запросов. Каждый из них занимает соединение и ресурсы API-шлюза, что при достижении порогов ограничения по соединениям приводит к отказу в обслуживании.

Закон Литтла носит универсальный характер: он не зависит от распределения времён поступления запросов и от внутренней структуры системы массового обслуживания @little1961. Из уравнения @eq-little прямо следует, что уменьшение $W_"cold"$ в $k$ раз пропорционально уменьшает число ожидающих запросов $L_"wait"$. Это делает сокращение времени холодного старта первоочередной задачей при проектировании масштабируемого ML-инференса.

=== Вероятность холодного старта при стратегии scale-to-zero

В бессерверных системах с автомасштабированием до нуля применяется механизм keep-alive: реплика сервиса поддерживается в активном состоянии в течение времени $T_"keep"$ после последнего запроса, после чего освобождается. Если поток входящих запросов к конкретной модели описывается пуассоновским процессом с интенсивностью $lambda$, то вероятность холодного старта для произвольного запроса определяется формулой @little1961:

$ P_"cold" = e^(-lambda dot T_"keep"), $ <eq-pcold>

где $T_"keep"$ --- время поддержания реплики в активном состоянии после последнего запроса.

При малой нагрузке $lambda -> 0$, и $P_"cold" -> 1$: практически каждый запрос инициирует холодный старт. Это характерно для длинного «хвоста» редко используемых специализированных моделей. Обратно, при высокой нагрузке $lambda -> infinity$, и $P_"cold" -> 0$: реплики постоянно активны.

Из формулы @eq-pcold следует, что при фиксированном целевом SLA на максимально допустимую вероятность холодного старта $P_"cold" lt.eq epsilon$ требуемое время поддержания реплики определяется как:

$ T_"keep" >= -(ln epsilon)/lambda. $ <eq-keep-alive>

Для модели с $lambda = 0.01$ запр./с и допустимым $epsilon = 0.05$ (5% вероятность холодного старта) получаем $T_"keep" >= 299$ с, то есть около 5 минут. За это время простаивающая GPU потребляет энергию и занимает слот, за который взимается плата.

=== Дилемма scale-to-zero и over-provisioning

Из приведённого анализа вытекает фундаментальная дилемма, с которой сталкиваются операторы ML-платформ. Стратегия scale-to-zero обеспечивает оптимальное использование ресурсов в периоды низкой нагрузки, однако при высоком $W_"cold"$ делает невозможным соблюдение SLA на задержку первого токена.

Альтернатива --- over-provisioning, постоянное поддержание пула «тёплых» реплик --- устраняет задержку, но аннулирует экономическое преимущество бессерверной модели. Для модели, которая обрабатывает в среднем один запрос в час, постоянное удержание одной активной реплики с GPU-ускорителем создаёт крайне низкую утилизацию оборудования и высокую удельную стоимость обработки запроса.

Оптимальным является третий путь: снижение $W_"cold"$ до уровня, при котором допустимо использовать небольшой keep-alive или даже полный scale-to-zero без критических нарушений SLA. Если холодный старт занимает 30 секунд вместо 10 минут, то допустимое время ожидания первого запроса переходит в категорию приемлемых задержек для многих производственных сценариев.

Таблица @tab-cold-start-components иллюстрирует ориентировочный вклад различных компонентов в суммарную задержку холодного старта для модели с 8 и 70 миллиардами параметров при типичных конфигурациях.

#figure(
  table(
    columns: 4,
    stroke: (x, y) => if y == 0 or y == 1 { (bottom: 0.5pt) } else { none },
    table.header(
      [Фаза],
      table.cell(colspan: 2)[Типичная длительность],
      [Зависимость от размера],
    ),
    [], [8B модель], [70B модель], [],
    [$T_"schedule"$ (планирование)], [5--30 с],   [5--30 с],   [Нет],
    [$T_"pull"$ (загрузка образа)],  [2--5 мин],  [2--5 мин],  [Слабая],
    [$T_"unpack"$ (распаковка)],     [2--4 мин],  [2--4 мин],  [Слабая],
    [$T_"download"$ (загрузка весов)], [2--4 мин], [15--25 мин], [Линейная],
    [$T_"load"$ (в GPU-память)],     [1--2 мин],  [5--10 мин], [Линейная],
    [$T_"start"$ (итого)],           [7--15 мин], [29--44 мин], [Значимая],
  ),
  caption: [Ориентировочный вклад фаз холодного старта (оценочные данные)],
) <tab-cold-start-components>

Из таблицы @tab-cold-start-components видно, что при переходе от 8B к 70B параметров компоненты $T_"download"$ и $T_"load"$ вносят наибольший вклад в прирост суммарной задержки. Именно на эти компоненты должны быть направлены основные архитектурные оптимизации.

=== Влияние на пользовательский опыт

С точки зрения восприятия пользователем задержка свыше 3 секунд при обращении к веб-сервису существенно снижает вероятность продолжения взаимодействия. Для голосовых ассистентов и интерактивных чат-ботов практически любая задержка первого токена сверх 2--3 секунд воспринимается негативно. Холодный старт ML-сервиса, длящийся несколько минут, делает продукт практически непригодным к использованию в периоды низкой нагрузки при стратегии scale-to-zero. Это вынуждает продуктовые команды выбирать между ухудшением UX и значительными дополнительными затратами на инфраструктуру.

== Требования к системе оптимизации холодного старта

На основании проведённого анализа механизма холодного старта, его количественных характеристик и экономических последствий сформулированы следующие требования к системе оптимизации.

=== Функциональные требования

*ФТ-1. Минимизация задержки первого чтения данных модели.* Система должна предоставлять первые байты данных модели в наиболее короткие сроки, не дожидаясь полной загрузки модели из внешнего источника. Для этого необходима поддержка потоковой или ленивой загрузки с возможностью начать инференс до полного завершения скачивания.

*ФТ-2. Повторное использование уже загруженных весов.* Если файлы модели или их фрагменты уже присутствуют на дисках узлов кластера, новые реплики должны получать данные из этого локального или соседнего источника без обращения к внешнему репозиторию. Повторная загрузка идентичных байт из интернета при наличии их в кластере недопустима.

*ФТ-3. Прозрачность для ML-runtime.* Сервисы vLLM, TGI, Triton Inference Server и аналогичные не должны требовать модификации: они должны видеть обычный файловый путь в файловой системе и работать со стандартными POSIX-операциями чтения.

*ФТ-4. Поддержка больших файлов весов.* Система должна корректно работать с отдельными файлами весов размером от сотен мегабайт до нескольких сотен гигабайт, включая формат safetensors и бинарные форматы PyTorch. Обязательна поддержка произвольного доступа по смещению (range reads).

*ФТ-5. Горизонтальное масштабирование.* Производительность системы не должна деградировать при добавлении новых worker-нод и увеличении числа одновременно запущенных реплик инференса. Добавление узлов должно линейно увеличивать суммарный объём локального кэша.

*ФТ-6. Локальность данных и приоритет источников.* При обслуживании запроса на данные должен соблюдаться следующий приоритет источников: первый --- локальный диск той же ноды; второй --- диски соседних нод кластера по внутренней сети; третий --- внешний репозиторий модели.

=== Нефункциональные требования

*НТ-1. Отказоустойчивость при падении узла.* Выход одной или нескольких worker-нод из строя не должен делать модель полностью недоступной, при условии что её данные присутствуют на других нодах или доступны через fallback на внешний репозиторий.

*НТ-2. Устранение единой точки отказа (SPOF).* Архитектура не должна содержать единственного центрального компонента, недоступность которого делает систему нефункциональной. Это требование непосредственно исключает использование одного NFS-сервера в качестве единственного хранилища весов.

*НТ-3. Контролируемые накладные расходы.* Объём метаданных и управляющего трафика не должен создавать значимой нагрузки на сеть кластера или перегружать реестр метаданных. Накладные расходы на один запрос данных должны составлять пренебрежимо малую долю по сравнению с полезной нагрузкой.

*НТ-4. Совместимость с Kubernetes.* Все компоненты системы должны развёртываться как стандартные ресурсы Kubernetes --- DaemonSet для агентов на каждой ноде, Deployment для управляющих компонентов, стандартные PVC/PV для постоянного хранения --- без модификации ядра операционной системы или kubelet.

Совокупность сформулированных требований определяет целевые свойства архитектуры, которая должна обеспечивать приоритет локальных данных, прозрачность для runtime-сред и отказоустойчивость без центральной точки отказа.

== Выводы по разделу

Проведённый анализ позволяет сформулировать следующие выводы.

Рост размеров ML-моделей за период с 2012 по 2024 год на три-четыре порядка превратил холодный старт из второстепенной инфраструктурной задержки в архитектурный приоритет. Компоненты $T_"download"$ и $T_"load"$ из формулы @eq-cold-start доминируют в суммарном времени инициализации и напрямую определяются наличием локально кэшированных весов.

Применение закона Литтла (@eq-little) показывает, что линейное снижение $W_"cold"$ пропорционально снижает число ожидающих запросов $L_"wait"$, что критически важно при burst-масштабировании. Формула @eq-pcold демонстрирует, что для редко запрашиваемых моделей вероятность холодного старта стремится к единице вне зависимости от политики keep-alive, что оправдывает применение архитектурных механизмов ускорения.

Классическая схема с `emptyDir` не переиспользует уже загруженные веса между репликами и нодами, что при масштабировании приводит к $N$-кратному увеличению нагрузки на внешний репозиторий. Сформулированные функциональные и нефункциональные требования определяют необходимые свойства системы: локальность данных, прозрачность для ML-runtime, горизонтальная масштабируемость, отказоустойчивость без SPOF.

Центральный вывод раздела состоит в том, что ускорять необходимо не вычислительную фазу инференса, а подготовительную: доставку, кэширование и представление весов модели приложению. Это требует специализированной архитектуры хранения данных, ориентированной на локальность, P2P-передачу между нодами и прозрачный файловый интерфейс для runtime-окружений.
