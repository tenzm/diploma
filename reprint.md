# Reprint

Страницы на перепечать для `diploma-latex-template/mablinov/main.pdf` после правок от `2026-06-25`:

- `19` — заголовок раздела в верхнем регистре выровнен по центру.
- `30-32` — подзаголовки `ФТ-*` и `НТ-*` получили явную нумерацию и убрана точка в конце.
- `34` — заголовок раздела в верхнем регистре выровнен по центру.
- `48` — заголовок раздела в верхнем регистре выровнен по центру.
- `50-63` — подписи к фрагментам кода перенесены под код и выровнены по центру; внутри главы произошёл локальный перенос строк.
- `94` — заголовок раздела в верхнем регистре выровнен по центру.

Итого на перепечать: `21` страница.

Проверка выполнена сравнением рендеров `tmp/reprint/main-before.pdf` и актуального `diploma-latex-template/mablinov/main.pdf`. Общее количество страниц не изменилось: `114`.
